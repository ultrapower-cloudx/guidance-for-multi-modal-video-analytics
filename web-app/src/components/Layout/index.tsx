import BaseLayout from "./BaseLayout";
import CustomLogin from "./CustomLogin";
import LayoutWithCognito from "./LayoutWithCognito";

const Layout = {
  AuthWithCognito: LayoutWithCognito,
  CustomizedAuth: CustomLogin,
  Base: BaseLayout,
};

export default Layout;
